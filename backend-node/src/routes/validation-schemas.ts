import {
  loginSchema, registerSchema, sendMessageSchema, updateStatusSchema,
  assignSchema, transferSchema, handoffSchema, handoffActionSchema, addNoteSchema,
  aiSettingsSchema, faqSchema, faqUpdateSchema, sentimentSchema, autoReplySchema,
  suggestionsSchema, recommendationsSchema,
  cannedSchema, cannedUpdateSchema,
  contactSchema, contactUpdateSchema, dealSchema, dealUpdateSchema,
  campaignSchema, campaignUpdateSchema,
  subscriptionPlanSchema, whitelabelSchema, channelUpdateSchema,
  webhookSimulateSchema, webhookReplySchema, trackClickSchema,
} from "../middleware/validation";

export {
  loginSchema, registerSchema, sendMessageSchema, updateStatusSchema,
  assignSchema, transferSchema, handoffSchema, handoffActionSchema, addNoteSchema,
  aiSettingsSchema, faqSchema, faqUpdateSchema, sentimentSchema, autoReplySchema,
  suggestionsSchema, recommendationsSchema,
  cannedSchema, cannedUpdateSchema,
  contactSchema, contactUpdateSchema, dealSchema, dealUpdateSchema,
  campaignSchema, campaignUpdateSchema,
  subscriptionPlanSchema, whitelabelSchema, channelUpdateSchema,
  webhookSimulateSchema, webhookReplySchema, trackClickSchema,
};
